[phases.install]
cmds = ["/app/.venv/bin/pip install -r requirements.txt"]

[phases.build]
cmds = ["/app/.venv/bin/python -m playwright install chromium", "/app/.venv/bin/python -m playwright install-deps chromium"]

[start]
cmd = "/app/.venv/bin/python app.py"
